<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login - Janvi Sender App</title>
  
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  
  <style>
    body { 
      font-family: 'Inter', sans-serif; 
      display: flex; 
      flex-direction: column; /* Stack content and footer */
      justify-content: center; 
      align-items: center; 
      min-height: 100vh; 
      background-color: #f0f2f5; 
      margin: 0;
      color: #333;
    }
    
    .login-container { 
      background: #fff; 
      border-radius: 12px; 
      padding: 40px; 
      width: 100%;
      max-width: 400px; 
      box-shadow: 0 10px 25px rgba(0,0,0,0.05); 
      border: 1px solid #e2e8f0;
    }

    /* Branding Styles */
    .brand-header {
      text-align: center;
      margin-bottom: 30px;
    }
    .logo-img {
      height: 60px; 
      width: auto;
      margin-bottom: 15px;
      background-color: white;
      border-radius: 8px;
      padding: 5px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.05);
    }
    .brand-title {
      font-size: 1.5em;
      font-weight: 800;
      color: #1e3a8a; 
      margin-top: 5px;
      letter-spacing: -0.5px;
    }

    form div { margin-bottom: 20px; }
    label { display: block; margin-bottom: 8px; font-weight: 600; color: #475569; font-size: 0.95em; }
    input[type="text"], input[type="password"] {
      width: 100%;
      padding: 12px;
      border: 1px solid #cbd5e1;
      border-radius: 6px;
      box-sizing: border-box; 
      font-size: 1em;
      transition: border-color 0.2s;
    }
    input:focus { border-color: #4f46e5; outline: none; }

    button { 
      padding: 12px 20px; 
      background-color: #4f46e5; /* Match Brand Blue */
      color: white; 
      border-radius: 6px; 
      border: none;
      font-size: 1em;
      cursor: pointer;
      width: 100%;
      font-weight: 600;
      transition: background-color 0.2s;
      margin-top: 10px;
    }
    button:hover { background-color: #4338ca; }
    
    p { text-align: center; margin-top: 25px; color: #64748b; font-size: 0.95em; }
    a { color: #4f46e5; text-decoration: none; font-weight: 600; }
    a:hover { text-decoration: underline; }

    /* Alert Box Styling */
    .alert {
      padding: 15px;
      margin-bottom: 25px;
      border-radius: 6px;
      font-weight: 500;
      width: 100%;
      box-sizing: border-box;
      font-size: 0.95em;
      line-height: 1.5;
    }
    .alert-success { background-color: #dcfce7; color: #166534; border: 1px solid #bbf7d0; }
    .alert-error { background-color: #fee2e2; color: #991b1b; border: 1px solid #fecaca; }
    .alert-warning { background-color: #fef3c7; color: #92400e; border: 1px solid #fde68a; }

    /* Footer */
    footer {
      margin-top: 30px;
      color: #94a3b8;
      font-size: 0.85em;
      text-align: center;
    }
  </style>
</head>
<body>

  <div class="login-container">
    <div class="brand-header">
        <img src="https://www.janvipackaging.online/images/Janvi-Packaging-Logo" alt="Janvi Packaging Logo" class="logo-img">
        <div class="brand-title">Janvi Sender App</div>
    </div>
    
    <% if (success_msg && success_msg.length > 0) { %>
      <div class="alert alert-success">
        <%= success_msg %>
      </div>
    <% } %>

    <% if (error && error.length > 0) { %>
      <div class="alert alert-error">
        <%= error %>
      </div>
    <% } %>

    <% if (error_msg && error_msg.length > 0) { %>
        <% 
            /* Flexible check for 'pending' status messages */
            const isUnapproved = error_msg.toString().toLowerCase().includes('pending');
            const alertClass = isUnapproved ? 'alert-warning' : 'alert-error';
        %>
      <div class="alert <%= alertClass %>">
        <%= error_msg %>
        <% if (isUnapproved) { %>
            <br><span style="font-size: 0.9em; opacity: 0.9;">Please check back later or contact the admin.</span>
        <% } %>
      </div>
    <% } %>

    <form action="/users/login" method="POST">
      <div>
        <label for="username">Username</label>
        <input type="text" name="username" id="username" placeholder="Enter your username" required>
      </div>
      <div>
        <label for="password">Password</label>
        <input type="password" name="password" id="password" placeholder="Enter your password" required>
      </div>
      <button type="submit">Login</button>
    </form>
    
    <p>
      Don't have an account? <a href="/users/register">Register Here</a>
    </p>
  </div>

  <footer>
    &copy; <%= new Date().getFullYear() %> Janvi Packaging App. All rights reserved.
  </footer>

</body>
</html>