<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Inbox</title>
  
  <style>
    body { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif; margin: 40px; background-color: #f4f7f6; }
    h1 { color: #333; }
    a { color: #007bff; text-decoration: none; }
    a:hover { text-decoration: underline; }

    /* Alert Box Styling */
    .alert { padding: 15px; margin-bottom: 20px; border-radius: 5px; font-weight: 500; }
    .alert-success { background-color: #d1e7dd; color: #0f5132; border: 1px solid #badbcc; }
    .alert-error { background-color: #f8d7da; color: #842029; border: 1px solid #f5c2c7; }

    /* Message List Styling */
    .message-list { list-style-type: none; padding: 0; }
    .message-item {
      background: #fff;
      border-radius: 8px;
      padding: 15px 20px;
      margin-bottom: 15px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.05);
    }
    .message-header {
      border-bottom: 1px solid #eee;
      padding-bottom: 10px;
      margin-bottom: 10px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    .message-contact { font-weight: 600; font-size: 1.1em; color: #333; }
    .message-contact span { font-weight: 400; color: #555; font-size: 0.9em; }
    .message-time { font-size: 0.9em; color: #777; }
    .message-body { font-size: 1em; color: #222; line-height: 1.5; }
    .message-company { font-size: 0.85em; color: #007bff; font-weight: 500; }
    
    /* Button Styles */
    .btn {
      padding: 5px 10px;
      border-radius: 4px;
      text-decoration: none;
      color: white;
      font-size: 0.9em;
      font-weight: 500;
      margin-right: 15px;
    }
    .btn-read { background-color: #28a745; }
    .btn-read:hover { background-color: #218838; }
    .btn-read-all {
      background-color: #17a2b8;
      color: white;
      text-decoration: none;
      padding: 8px 12px;
      border-radius: 5px;
      font-weight: 600;
      display: inline-block;
      margin-left: 20px;
    }
    .btn-read-all:hover { background-color: #138496; }

  </style>
</head>
<body>

  <h1>Inbox - Customer Replies</h1>
  <p>
    <a href="/">&larr; Back to Dashboard</a>
    <a href="/inbox/readall" class="btn-read-all">Mark All as Read</a>
  </p>

  <% if (success_msg && success_msg.length > 0) { %>
    <div class="alert alert-success">
      <%= success_msg %>
    </div>
  <% } %>
  <% if (error_msg && error_msg.length > 0) { %>
    <div class="alert alert-error">
      <%= error_msg %>
    </div>
  <% } %>
  <ul class="message-list">
    
    <% messages.forEach(function(message) { %>
      <li class="message-item" style="<%= message.isRead ? '' : 'border-left: 5px solid #007bff;' %>">
        <div class="message-header">
          <div class="message-contact">
            <%= message.contact ? (message.contact.name || message.contact.phone) : 'Unknown Contact' %>
            <span class="message-company">
              (<%= message.company ? message.company.name : 'Unknown Company' %>)
              <% if (!message.isRead) { %>
                <span style="color: #dc3545; font-weight: 600;"> (NEW)</span>
              <% } %>
            </span>
          </div>
          <div style="display: flex; align-items: center;">
            <% if (!message.isRead) { %>
              <a href="/inbox/read/<%= message._id %>" class="btn btn-read">Mark as Read</a>
            <% } %>
            <div class="message-time">
              <%= new Date(message.createdAt).toLocaleString() %>
            </div>
          </div>
        </div>
        <div class="message-body">
          <%= message.body %>
        </div>
      </li>
    <% }); %>

    <% if (messages.length === 0) { %>
      <li class="message-item">
        <div class="message-body">
          You have no messages yet.
        </div>
      </li>
    <% } %>

  </ul>

</body>
</html>